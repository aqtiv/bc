sudo: required
dist: trusty

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y openjdk-8-jdk gradle

before_script: 
  - gradle --version

script:
  - jdk_switcher use openjdk8
  - gradle build
